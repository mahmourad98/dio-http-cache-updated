name: dio_http_cache_example
description: A demo of the dio_http_cache package.
version: 1.0.0+1
publish_to: none

environment:
  sdk: '>=2.17.0 <3.0.0'
  flutter: '>=2.17.0 <3.10.0'

dependencies:
  flutter:
    sdk: flutter
  dio_http_cache:
    path: ../

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
